# Repository Rename

This repository was renamed to `otcan/oxrm` before public launch.

The product name is oXRM.
The CLI command is `./oxrm`.

Old repository links should redirect, but new documentation should use the new
URL.
